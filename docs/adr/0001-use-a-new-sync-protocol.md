---
status: proposed
---

# 使用独立同步协议

本项目替代 Seafile 的文件同步能力，但不兼容现有 Seafile 服务端或客户端协议。新协议只吸收内容寻址、不可变快照、缺失对象协商和冲突副本等思想，避免继承 SHA-1、历史端点、双 token 控制面及许可证耦合；代价是现有 Seafile 客户端不能连接本项目，也不能直接迁移协议级对象。

考虑过兼容 Seafile 协议和移植其实现；前者会冻结历史约束，后者引入 C/GLib 及 GPL/AGPL 边界，因此否决。只有出现必须无缝接入现有 Seafile 客户端的明确商业需求时重新评估。
