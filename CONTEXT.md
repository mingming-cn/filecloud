# Filecloud

Filecloud 是以资料库为同步单位、在多个设备之间收敛文件树的系统。本词汇表只定义领域语言，不描述实现。

## 领域语言

**资料库（Library）**：
由一个用户拥有、可独立绑定到本地目录并同步的文件集合。每个资料库拥有独立身份和版本历史。
_避免_：仓库、Repo、共享目录

**工作目录（Worktree）**：
某个设备上与一个资料库绑定的普通本地目录。一个工作目录不能同时绑定多个资料库。
_避免_：缓存目录、挂载点

**快照（Snapshot）**：
资料库在一个确定时刻的完整、不可变文件树，由一次提交引用。
_避免_：当前目录、备份

**提交（Commit）**：
描述一次资料库版本变更的不可变记录，引用一个快照及其父提交。
_避免_：上传任务、操作日志

**Head**：
资料库当前已发布提交的唯一指针。改变 Head 是一次带前置版本检查的原子操作。
_避免_：最新文件、客户端版本

**同步基线（Sync Base）**：
某个工作目录最近一次确认已与服务端一致的提交。它是比较本地与远端变化的共同祖先。
_避免_：本地 Head、缓存版本

**候选提交（Candidate Commit）**：
客户端根据同步基线和当前工作目录创建、但尚未发布为 Head 的提交。
_避免_：草稿文件、临时上传

**冲突副本（Conflict Copy）**：
并发变化无法无损合并时，为保留本地内容而创建的另一路径文件。原路径遵循当前远端版本。
_避免_：覆盖文件、备份文件

**内容对象（Content Object）**：
以内容摘要为身份的不可变对象，具体包括内容块、文件对象、目录对象和提交。
_避免_：数据库记录、附件

**内容块（Block）**：
普通文件按固定上限切分后的不可变字节片段。块身份由其字节内容决定。
_避免_：分片任务、上传片段

**收敛（Convergence）**：
客户端完成同步后，工作目录表示的快照、同步基线和服务端 Head 指向同一内容。
_避免_：上传完成、下载完成
